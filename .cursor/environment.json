{
  "name": "HouseHunting",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "dev",
      "command": "bash .cursor/start.sh && npm run dev",
      "description": "Ensures PostgreSQL is up, then runs the Next.js dev server on http://localhost:43123 (reads DATABASE_URL from .env)."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 43123
    }
  ]
}
